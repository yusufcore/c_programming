#include <stdio.h>

int main(){             // infinte loop.
    for(;;){           
        printf("Hello\n");
    }
}
// for loop syntax -->  ; ;  are must. not even a single semi-colon can be missed else it will give an error.