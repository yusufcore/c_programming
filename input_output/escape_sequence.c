#include <stdio.h>

int main(){

    int b = 1500;
    float h = 200.50;
    char g = 'A';
    printf("Basic = %d\n, HRA = %f\n, Grade = %c\n", b,h,g);
    return 0;   
    // note -> \n is an escape sequence which is used to print the next output in a new line.
}